# API Reference

The command line interface accepts a dataset path and a query.

## Search Command

Use the search command to look up documents.
Results are ranked and returned with section context.

## Output Format

Each result shows the path, section title, score, and snippet.
